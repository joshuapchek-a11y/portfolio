# HTML & CSS Workspace

This is a simple starter workspace for HTML and CSS development.

## Files
- `index.html`: Main HTML file
- `styles.css`: Main CSS file

## Usage
Open this folder in VS Code. Edit `index.html` and `styles.css` to start building your website.
